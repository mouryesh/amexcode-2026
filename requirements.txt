pydantic>=2.5
pyyaml>=6.0
fastapi>=0.110
uvicorn>=0.27
langgraph>=0.2
pytest>=8.0
httpx>=0.27
scikit-learn>=1.4
sentence-transformers>=3.0
joblib>=1.3
python-dotenv>=1.0
# Optional LLM providers — only needed when LLM_PROVIDER is set to one of these.
# anthropic>=0.40
openai>=1.30   # also used for provider="groq" (OpenAI-compatible API)
