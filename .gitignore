# Python
__pycache__/
*.pyc

# Secrets — never commit. .env.example is the tracked template.
.env
.env.*
!.env.example

# SQLite databases (generated at runtime / self-checks)
*.db
_*_selfcheck.db
servicing.db

# runtime db
servicing.db*
.pytest_cache/
